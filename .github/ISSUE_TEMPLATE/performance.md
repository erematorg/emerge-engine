---
name: Performance issue
about: Solver is slower than expected or hits a scaling wall
labels: performance
---

**What are you simulating** (particle count, grid_res, materials, features)

**Measured time per step** (use `Simulation::diagnostics_snapshot()` or a timer)

**Hardware** (CPU cores, GPU model if using `--features gpu`)

**What result did you expect**
